[project]
name = "indice-gansito"
version = "0.1.0"
description = "Ingesta de Quién es Quién en los Precios hacia la zona raw"
requires-python = ">=3.13,<3.14"
dependencies = [
    "polars>=1.0",
    "pyyaml>=6.0",
]

# deploy.py no entra aquí: sus dependencias van inline en `uv run --with`,
# porque sólo las necesita el workflow de despliegue.
[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.6",
]

[tool.pytest.ini_options]
pythonpath = ["scripts"]
testpaths = ["tests"]

[tool.ruff]
line-length = 96
target-version = "py313"
# El Python de los notebooks corre en Fabric, donde `spark` y `notebookutils` son
# globales del runtime: linteado aquí es puro F821. Ver decisión #7.
extend-exclude = ["fabric"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
