name: ci

# Sólo cubre el Python que corre en Actions. El que corre en Fabric se prueba en
# nb_90_pruebas, dentro del notebook: ver decisión #7. De `fabric/` aquí no se revisa
# el código sino la metadata: que ningún notebook traiga lakehouse por defecto.

on:
  push:
    branches: [dev, main]
    paths: &python
      - 'scripts/**'
      - 'tests/**'
      - 'fabric/**'
      - 'objetivo.yml'
      - 'pyproject.toml'
      - '.github/workflows/ci.yml'
  pull_request:
    paths: *python

permissions:
  contents: read

jobs:
  probar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Instala uv
        uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true

      - run: uv run --python 3.11 ruff check .
      - run: uv run --python 3.11 ruff format --check .
      - run: uv run --python 3.11 pytest -q
